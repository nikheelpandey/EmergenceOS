"""
EmergenceOS Test Suite

This package contains every automated test for EmergenceOS.

The tests are organized into multiple layers:

    Unit Tests
        Verify a single component in isolation.

    Integration Tests
        Verify collaboration between multiple components.

    System Tests
        Boot the operating system and verify end-to-end behavior.

    Invariant Tests
        Protect architectural guarantees that should never change.

The purpose of this suite is not merely achieving high code coverage.
Instead, it exists to prevent regressions in the EmergenceOS kernel while
allowing aggressive internal refactoring.
"""