name: evaluator
version: 1.0.0
description: Evaluates research quality via LLM scoring.
entrypoint: evaluator:run
runner: python
required_capabilities:
  - state.read
  - state.write
  - event.publish
  - tool.llm
supported_events:
  - evaluation.completed
  - tool.requested
  - tool.completed
